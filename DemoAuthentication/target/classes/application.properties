spring.application.name=aunthenticationImpl

# MySQL Database Configuration
#spring.datasource.url=jdbc:mysql://localhost:3306/auth_db?useSSL=false&serverTimezone=UTC
spring.datasource.url=jdbc:mysql://localhost:3306/auth_db?useSSL=false&serverTimezone=UTC&allowPublicKeyRetrieval=true
spring.datasource.username=root
spring.datasource.password=root
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver


# JPA and Hibernate Configuration
spring.jpa.database-platform=org.hibernate.dialect.MySQLDialect
spring.jpa.hibernate.ddl-auto=update
spring.jpa.show-sql=true

# Spring Boot Security Default User Disable
spring.security.user.name=radha
spring.security.user.password=radha0308

# JWT Configuration
# it should be at least of 32 characters
jwt.secret=MySuperSecretKeyThatIsAtLeast32CharactersLong
jwt.expirationTime=3600000